# 核心代码速查

本文档整理了项目中最关键的代码片段，方便快速定位和理解核心逻辑。

---

## 一、DeviceClient 核心逻辑

### 1. HTTPS 自签名证书跳过

```dart
static Dio _buildDio(Dio? override) {
  if (override != null) return override;
  final dio = Dio();
  // 跳过内网自签名证书校验
  (dio.httpClientAdapter as dynamic).onHttpClientCreate =
      (HttpClient client) {
    client.badCertificateCallback = (cert, host, port) => true;
    return client;
  };
  return dio;
}
```

**作用**：允许连接使用自签名证书的内网 HTTPS 服务器。

---

### 2. 地址规整化

```dart
static String normalizeBase(String input) {
  var s = input.trim();
  if (s.isEmpty) return '';
  if (!s.startsWith('http://') && !s.startsWith('https://')) {
    s = 'https://$s';  // 默认 HTTPS
  }
  // 去掉结尾斜杠
  return s.replaceAll(RegExp(r'/+$'), '');
}
```

**输入示例**：`10.192.225.229`  
**输出结果**：`https://10.192.225.229`

---

### 3. 视频列表拉取（兼容两种格式）

```dart
Future<List<RemoteVlog>> fetchList(String baseUrl) async {
  final base = normalizeBase(baseUrl);
  final resp = await _dio.get(
    '$base/list',
    options: Options(
      sendTimeout: const Duration(seconds: 8),
      receiveTimeout: const Duration(seconds: 8),
      responseType: ResponseType.json,
    ),
  );
  final data = resp.data;
  // 兼容两种格式：{"videos":[...]} 或直接 [...]
  final rawList = data is Map ? (data['videos'] ?? data['list'] ?? []) : data;
  final list = <RemoteVlog>[];
  if (rawList is List) {
    for (final e in rawList) {
      if (e is Map) {
        final v = RemoteVlog.fromJson(Map<String, dynamic>.from(e));
        if (v.name.isNotEmpty) list.add(v);
      } else if (e is String && e.isNotEmpty) {
        list.add(RemoteVlog(name: e, size: 0));
      }
    }
  }
  return list;
}
```

**关键逻辑**：
- 兼容对象包数组格式：`{"videos": [...]}`
- 兼容顶层数组格式：`[...]`
- 兼容字符串数组格式：`["a.mp4", "b.mp4"]`

---

### 4. 视频下载（带 Bearer Token）

```dart
Future<String> download(
  String baseUrl,
  RemoteVlog vlog, {
  DownloadProgress? onProgress,
}) async {
  final docs = await getApplicationDocumentsDirectory();
  final outDir = Directory(p.join(docs.path, 'vlogs'));
  if (!await outDir.exists()) await outDir.create(recursive: true);

  final safeName = vlog.name.replaceAll(RegExp(r'[^\w\.\-]'), '_');
  final destPath =
      p.join(outDir.path, 'dev_${DateTime.now().millisecondsSinceEpoch}_$safeName');

  // 优先用后端给的完整 URL，否则回退旧协议
  final downloadUrl = vlog.downloadUrl?.isNotEmpty == true
      ? vlog.downloadUrl!
      : '${normalizeBase(baseUrl)}/download?name=${Uri.encodeComponent(vlog.name)}';

  await _dio.download(
    downloadUrl,
    destPath,
    options: Options(
      receiveTimeout: const Duration(minutes: 5),
      headers: {'Authorization': 'Bearer $_apiToken'},  // 认证
    ),
    onReceiveProgress: (received, total) {
      if (onProgress != null) {
        onProgress(total > 0 ? received / total : null);
      }
    },
  );

  final title = vlog.title.isNotEmpty
      ? vlog.title
      : p.basenameWithoutExtension(vlog.name);
  await vlogStore.add(VlogRecord(
    videoPath: destPath,
    title: title,
    day: '',
    createdAt: DateTime.now().millisecondsSinceEpoch,
  ));
  return destPath;
}
```

**关键点**：
- 优先使用后端返回的完整 URL（`vlog.downloadUrl`）
- Bearer Token 认证：`Authorization: Bearer <token>`
- 文件名加时间戳前缀，避免冲突
- 下载完成后自动入库

---

## 二、DevicePage 自动接收逻辑

### 1. 轮询机制

```dart
void _startPollingIfNeeded() {
  _pollTimer?.cancel();
  if (!_autoReceive || !_connected) return;
  _pollTimer = Timer.periodic(const Duration(seconds: 10), (_) => _refreshAndAutoFetch());
}

Future<void> _refreshAndAutoFetch() async {
  if (!_connected) return;
  try {
    final list = await deviceClient.fetchList(_ipCtrl.text.trim());
    if (!mounted) return;
    setState(() => _remote = list);
    if (_autoReceive) _autoFetchNew();
  } catch (_) {
    // 轮询失败静默
  }
}
```

**逻辑**：
- 每 10 秒调用一次 `/list`
- 更新远程视频列表
- 触发自动下载新视频
- 失败时静默（不打断用户）

---

### 2. 判重与自动下载

```dart
Future<void> _loadLocalNames() async {
  final all = await vlogStore.loadAll();
  final names = <String>{};
  for (final r in all) {
    final base = r.videoPath.split(RegExp(r'[\\/]')).last;
    final m = RegExp(r'^dev_\d+_(.+)$').firstMatch(base);
    if (m != null) names.add(m.group(1)!);
    names.add(r.title);
  }
  if (mounted) setState(() => _localNames = names);
}

bool _isNew(RemoteVlog v) {
  final stem = v.name.replaceAll(RegExp(r'\.[^.]+$'), '');
  return !_localNames.contains(v.name) && !_localNames.contains(stem);
}

Future<void> _autoFetchNew() async {
  for (final v in _remote) {
    if (_isNew(v) && !_downloading.containsKey(v.name)) {
      await _download(v, auto: true);
    }
  }
}
```

**判重逻辑**：
- 提取本地文件的原始文件名（去掉 `dev_时间戳_` 前缀）
- 同时记录所有标题
- 远程视频的 `name` 或去后缀 `stem` 存在于本地 → 判定为已下载

---

## 三、播放器与保存相册

### 视频初始化与循环播放

```dart
Future<void> _init() async {
  final c = VideoPlayerController.file(File(widget.videoPath));
  _controller = c;
  try {
    await c.initialize();
    await c.setLooping(true);  // 循环播放
    await c.play();
    if (mounted) setState(() => _ready = true);
  } catch (_) {
    if (mounted) setState(() => _ready = false);
  }
}
```

---

### 保存到相册

```dart
Future<void> _saveToGallery() async {
  setState(() => _saving = true);
  try {
    final has = await Gal.hasAccess();
    if (!has) await Gal.requestAccess();
    await Gal.putVideo(widget.videoPath, album: 'AI Vlog');  // 创建相册
    _snack('已保存到相册', ok: true);
  } catch (e) {
    _snack('保存失败：$e', ok: false);
  } finally {
    if (mounted) setState(() => _saving = false);
  }
}
```

**说明**：使用 `gal` 库，自动创建"AI Vlog"相册。

---

## 四、VlogStore 持久化

### 数据结构

```dart
class VlogRecord {
  final String videoPath;   // 本地文件完整路径
  final String title;       // 显示标题
  final String day;         // 关联日期（暂未使用）
  final int createdAt;      // 毫秒时间戳
}
```

---

### 存取逻辑

```dart
Future<List<VlogRecord>> loadAll() async {
  final prefs = await SharedPreferences.getInstance();
  final raw = prefs.getString(_kKey);
  if (raw == null || raw.isEmpty) return [];
  try {
    final list = jsonDecode(raw) as List;
    final records = list
        .map((e) => VlogRecord.fromJson(Map<String, dynamic>.from(e as Map)))
        .toList();
    // 按创建时间倒序
    records.sort((a, b) => b.createdAt.compareTo(a.createdAt));
    return records;
  } catch (_) {
    return [];
  }
}

Future<void> add(VlogRecord record) async {
  final all = await loadAll();
  all.insert(0, record);
  await _save(all);
}

Future<void> _save(List<VlogRecord> records) async {
  final prefs = await SharedPreferences.getInstance();
  await prefs.setString(_kKey, jsonEncode(records.map((r) => r.toJson()).toList()));
}
```

**存储方式**：SharedPreferences 存 JSON 字符串（单个键值对）。

---

## 五、关键常量与配置

### Bearer Token

```dart
// device_client.dart
static const _apiToken = '0a3a3873e84551cf78b2b59d3895b44fc39510d2dde64ad7';
```

**用途**：后端下载接口认证（写死在代码里，生产环境需改为可配置）。

---

### SharedPreferences 键

```dart
// device_page.dart
static const _kLastIp = 'last_device_ip';         // 上次连接的 IP
static const _kAutoReceive = 'auto_receive';       // 自动接收开关状态

// vlog_store.dart
static const _kKey = 'vlog_records';               // Vlog 记录 JSON 数组
```

---

### 轮询间隔

```dart
// device_page.dart
Timer.periodic(const Duration(seconds: 10), (_) => _refreshAndAutoFetch())
```

**说明**：每 10 秒拉取一次列表，可调整为更长间隔以节省流量。

---

## 六、错误处理

### 友好错误描述

```dart
static String describeError(Object e) {
  if (e is DioException) {
    switch (e.type) {
      case DioExceptionType.connectionTimeout:
      case DioExceptionType.sendTimeout:
      case DioExceptionType.receiveTimeout:
        return '连接超时：确认设备已连上手机热点、IP 填对了';
      case DioExceptionType.connectionError:
        return '连不上设备：检查 IP/端口，以及是否在同一网络';
      case DioExceptionType.badResponse:
        return '设备返回错误：HTTP ${e.response?.statusCode}';
      default:
        return '请求失败：${e.message ?? e.type.name}';
    }
  }
  return '出错：$e';
}
```

**作用**：把 Dio 抛出的异常转换为用户可理解的中文提示。

---

## 七、UI 关键交互

### 下载进度显示

```dart
Widget _remoteTile(RemoteVlog v) {
  final downloading = _downloading.containsKey(v.name);
  final prog = _downloading[v.name];
  final isNew = _isNew(v);
  return Card(
    child: ListTile(
      leading: Icon(Icons.movie_outlined,
          color: isNew ? Theme.of(context).colorScheme.primary : Colors.grey),
      subtitle: downloading
          ? Row(
              children: [
                Expanded(child: LinearProgressIndicator(value: prog)),
                const SizedBox(width: 8),
                Text(prog != null ? '${(prog * 100).round()}%' : '…',
                    style: const TextStyle(fontSize: 12)),
              ],
            )
          : Text(isNew ? v.sizeText : '已在本地'),
      trailing: downloading
          ? null
          : (isNew
              ? IconButton(icon: const Icon(Icons.download), onPressed: () => _download(v))
              : null),
    ),
  );
}
```

**效果**：
- 下载中：显示进度条 + 百分比
- 未下载：显示文件大小 + 下载按钮
- 已下载：显示"已在本地" + ✓ 图标

---

## 八、文件命名规则

### 下载文件命名

```dart
final safeName = vlog.name.replaceAll(RegExp(r'[^\w\.\-]'), '_');
final destPath = p.join(outDir.path, 'dev_${DateTime.now().millisecondsSinceEpoch}_$safeName');
```

**示例**：
- 远程文件：`xxx.mp4`
- 本地保存：`/data/.../vlogs/dev_1726329845123_xxx.mp4`

**目的**：
- `dev_` 前缀：标记来源（设备下载）
- 时间戳：避免文件名冲突
- `safeName`：过滤非法字符

---

### 导入文件命名

```dart
final destPath = p.join(outDir.path, 'import_${DateTime.now().millisecondsSinceEpoch}_$srcName');
```

**示例**：
- 源文件：`/sdcard/DCIM/video.mp4`
- 本地保存：`/data/.../vlogs/import_1726329845123_video.mp4`

**前缀**：`import_` 标记来源（手动导入）

---

## 九、性能优化点

1. **判重用 Set**：`_localNames` 是 `Set<String>`，查找 O(1)
2. **轮询失败静默**：避免频繁弹窗打断用户
3. **IndexedStack**：底部导航用 `IndexedStack` 保持页面状态
4. **懒加载**：`ListView.separated` 只渲染可见项

---

## 十、待优化项

1. **跨页面状态同步**：
   - 当前：下载完成后 Vlog 库需手动刷新
   - 方案：用 Riverpod `StateNotifier` 或 `ValueNotifier` 全局通知

2. **Token 配置化**：
   - 当前：硬编码在 `device_client.dart`
   - 方案：存到 SharedPreferences，设置页可修改

3. **断点续传**：
   - 当前：下载失败需重新开始
   - 方案：用 `dio` 的 `Range` 请求头实现续传

4. **视频缩略图**：
   - 当前：Vlog 库用图标占位
   - 方案：用 `video_thumbnail` 提取首帧

---

本文档整理了项目中最核心的代码逻辑，结合完整代码文件（见 `源代码/` 目录）和项目说明文档，可快速理解项目架构。
